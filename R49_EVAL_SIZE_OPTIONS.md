# R49 — آستانه‌هایی که در اندازهٔ فعلی «همه‌یا‌هیچ» هستند

**تصمیم لازم است، و من آن را نمی‌گیرم.** این سند سه گزینه را با هزینهٔ
اندازه‌گیری‌شده می‌گذارد. هیچ آستانه‌ای در هیچ گزینه‌ای **شل نمی‌شود**.

---

## مسئله، MEASURED

یک آستانهٔ درصدی روی مخرج کوچک، آستانهٔ درصدی **نیست** — یک دروازهٔ
«قبولی‌فقط‌با‌کمال» است که لباس درصد پوشیده. و این تفاوت در حکم **دیده
نمی‌شود**: هر دو «FAIL در برابر ۹۵» خوانده می‌شوند.

خروجی `scripts/threshold_resolution.py` روی اجرای ثبت‌شده:

| آستانه | حد | n فعلی | n لازم برای تحملِ **یک** خطا | وضعیت |
|---|---|---|---|---|
| `citation_correctness_pct_min` | ۹۵ | **۷** | **۲۰** | کمبود ۱۳ |
| `unsupported_claim_rate_pct_max` | ۳ | **۱۱** | **۳۴** | کمبود ۲۳ |
| `tool_call_schema_validity_pct_min` | ۹۸ | **۸** | **۵۰** | کمبود ۴۲ |
| `persian_fluency_regression_pct_max` | ۲ | **۲۳** | **۵۰** | کمبود ۲۷ |
| `correct_abstention_pct_min` | ۹۰ | **۹** | **۱۰** | کمبود **۱** |
| `deterministic_calc_correctness_pct_min` | ۱۰۰ | ۸ | — | **در هیچ n** |

**دو یافته که در گزارش قبلی‌ام نبود و باید تصحیح کنم:**

1. **مسئله دو آستانه نبود، شش آستانه است.** من R49 را بر اساس دو موردی که در
   حکم FAIL شدند ثبت کردم. اندازه‌گیری کامل نشان می‌دهد **۶ از ۱۲** آستانه
   رزولوشن ندارند — از جمله `tool_call_schema_validity` که **PASS شد**. آن PASS
   با ۸ فراخوان ابزار به دست آمد، در حالی که ۹۸٪ برای تحمل یک خطا **۵۰**
   فراخوان می‌خواهد. **یک PASS بدون رزولوشن هم بی‌معناست، نه فقط یک FAIL.**

2. **`deterministic_calc = 100` در هیچ اندازه‌ای قابل تحمل نیست.** ۱۰۰٪ ذاتاً
   کمال مطلق می‌خواهد؛ بزرگ‌کردن مجموعه آن را حل نمی‌کند. این یک آستانهٔ
   **صفرتحمل** است، مثل `fabricated_count = 0`، و باید همان‌طور فهمیده شود.

**نکتهٔ مهم:** رزولوشن، حکم ۲۰۲۶-۰۹-۰۵ را **عوض نمی‌کند**. ۴۲.۸۶٪ در n=۷ رد
می‌شود و در n=۲۰ هم رد می‌شود. رزولوشن دربارهٔ این است که آیا یک نتیجهٔ
**خوب‌ولی‌ناکامل در آینده** از یک نتیجهٔ بد قابل تشخیص خواهد بود.

---

## گزینه‌ها

### گزینهٔ A — ثبت محدودیت، بدون تغییر مجموعه (هزینه: صفر)

در `PROJECT_STATE` ثبت می‌شود که این ۶ آستانه در n فعلی «قبولی‌فقط‌با‌کمال»
هستند، و هر PASS یا FAIL آن‌ها با این قید خوانده می‌شود.

- ✅ صفر هزینه، صفر اجرای مدل
- ✅ صادقانه: هیچ عددی ادعای بیش از خودش نمی‌کند
- ❌ فاز ۵ روی آستانه‌هایی بنا می‌شود که نمی‌توانند بهبود را نشان دهند
- ❌ اگر مدل بعداً از ۴۲.۸۶٪ به ۸۵.۷۱٪ برسد، **باز هم FAIL** و هیچ‌کس نمی‌فهمد
  پیشرفتی شده

### گزینهٔ B — مجموعهٔ ارزیابی را بزرگ کنیم (توصیهٔ من)

MEASURED از خودِ اجرا: نسبت **۱.۵۷ ادعا به ازای هر پاسخ** (۱۱ ادعا / ۷ پاسخ).

| هدف | سطر پاسخ‌پذیر لازم | ادعای حاصل |
|---|---|---|
| n=۲۰ پاسخ | ۷ → **۲۰** (+۱۳) | ~۳۱ |
| n=۳۴ ادعا | ۷ → **۲۲** (+۱۵) | ~۳۴ ✅ |

پس **+۱۵ سؤال پاسخ‌پذیر** هر دو هدف را می‌پوشاند. با ۳ سطر abstain موجود،
بازوی rag می‌شود ۲۵ سطر.

**هزینهٔ اجرا، MEASURED نه تخمینی:** میانگین هر مورد rag در اجرای واقعی
**۲۹.۹ ثانیه** بود (۱۰ مورد = ۵.۰ دقیقه). پس:

| بازوی rag | زمان تولید |
|---|---|
| ۱۰ سطر (فعلی) | ۵.۰ دقیقه |
| **۲۵ سطر** | **~۱۲.۴ دقیقه** |

به‌علاوهٔ پروب ۴۹ ثانیه‌ای TTFT و زمان بارگذاری مدل (**UNKNOWN** — در هیچ فایل
شواهدی ثبت نشده، حدس نمی‌زنم).

**یعنی گسترش کامل بازوی rag ~۱۲ دقیقه اجرا می‌برد، نه ساعت‌ها.** این
کم‌هزینه‌ترین بخش کل پروژه است.

- ✅ آستانه‌ها **دست‌نخورده** می‌مانند و معنادار می‌شوند
- ✅ هزینهٔ اجرا کم و اندازه‌گیری‌شده
- ⚠️ کار من: ~۱۵ سؤال + سند شاهد جدید، با اعداد **واقعی** از منابع مجاز
- ⚠️ کار شما: یک اجرای ~۱۲ دقیقه‌ای، با تأیید صریح

**یک خطر که باید قبل از انجام بسنجم:** سندهای شاهد فعلی **fixture** هستند
(`FIX-AAPL-10K-2023` با `accession: FIXTURE-...`). اگر ۱۵ سؤال جدید را از
اعداد ساختگی بسازم، مجموعه بزرگ‌تر ولی **نه واقعی‌تر** می‌شود. باید از
پرکردن‌های واقعی SEC/FRED بیاید — و همان‌جاست که کار من زمان می‌برد، نه در
نوشتن سؤال.

### گزینهٔ C — آستانه‌ها را شل کنیم

**من این را رد می‌کنم و انجامش نمی‌دهم.** آستانه‌ها در ۲۰۲۶-۰۸-۱۰ **پیش‌تعهد**
شدند. شل‌کردن یکی از آن‌ها **بعد از دیدن اعداد**، یک pre-registration را به
تزئین تبدیل می‌کند و کل مبنای شواهدی این پروژه را بی‌اعتبار می‌کند. اگر شما
دستور دهید انجام می‌دهم، ولی توصیه‌ام صریحاً مخالف است و در DECISIONS با همین
عبارت ثبت خواهد شد.

---

## یک نکتهٔ ارزان که جدا از این سه گزینه است

`correct_abstention_pct_min = 90` تنها **۱ مورد** کمبود دارد (۹ → ۱۰). افزودن
**یک** سؤال abstain-محور، این آستانه را از «کمال مطلق» به «تحمل یک خطا»
می‌برد. این ارزان‌ترین بهبود رزولوشن در کل جدول است و مستقل از انتخاب شما
قابل انجام است.

---

## توصیهٔ من

**گزینهٔ B، و در همان اجرا مورد abstain را هم اضافه کنیم.** دلیل: هزینهٔ اجرا
MEASURED و ناچیز است (~۱۲ دقیقه)، آستانه‌ها دست‌نخورده می‌مانند، و بدون آن
فاز ۵ روی معیارهایی بنا می‌شود که نمی‌توانند پیشرفت را از شکست تشخیص دهند.

**قید صادقانه:** گزینهٔ B حکم FAIL را PASS نمی‌کند. دو آستانهٔ سخت‌افزاری
(decode و TTFT) هیچ ربطی به اندازهٔ مجموعه ندارند و طبق Q8 روی این سخت‌افزار
دست‌نیافتنی‌اند. گزینهٔ B معیارها را **قابل‌اندازه‌گیری** می‌کند، نه قابل‌قبول.
